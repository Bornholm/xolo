package component

import (
	"fmt"

	"github.com/bornholm/xolo/internal/http/handler/webui/templui/component/button"
	"github.com/bornholm/xolo/internal/http/handler/webui/templui/component/input"
)

// ── Extra Body Editor ─────────────────────────────────────────────────────────

// ExtraBodyEditor renders an editable key/value list for a model's extra_body.
// Each entry is injected verbatim into the outgoing request body. Values are
// typed by inference on submission (see parseExtraBodyFromForm): "true"/"false"
// become booleans, numeric strings become numbers, everything else stays a
// string.
//
// Form fields emitted:
//   - extra_body_count            (hidden, JS-managed)
//   - extra_body_k{i}_key
//   - extra_body_k{i}_value
templ ExtraBodyEditor(rows []ExtraBodyRow, readonly bool) {
	<div class="space-y-3">
		<div
			id="extra-body-rows"
			class="space-y-2"
			data-xolo-rows
			data-rows-template="extra-body-tpl"
			data-rows-count="extra-body-count"
		>
			for i, row := range rows {
				@extraBodyRow(i, row, readonly)
			}
		</div>
		// Hidden input : porte le nombre de lignes, tenu à jour par le module de
		// lignes répétables d'assets/js/xolo.js. Il transporte une valeur, ce n'est
		// pas un contrôle — templui n'a pas d'équivalent et n'en a pas besoin.
		<input type="hidden" id="extra-body-count" name="extra_body_count" value={ fmt.Sprintf("%d", len(rows)) }/>
		if !readonly {
			@button.Button(button.Props{
				Type:       "button",
				Variant:    button.VariantOutline,
				Class:      "w-full",
				Attributes: templ.Attributes{"data-xolo-row-add": "extra-body-rows"},
			}) {
				+ Ajouter un champ
			}
		}
	</div>
	// Modèle cloné à l'ajout d'une ligne. extraBodyRow avec idx -1 émet des noms
	// à trou (extra_body_k{i}_key, …) que la renumérotation réécrit.
	if !readonly {
		<template id="extra-body-tpl">
			@extraBodyRow(-1, ExtraBodyRow{}, false)
		</template>
	}
}

// extraBodyRow renders one key/value entry.
// idx = -1 → template row; names use placeholder "{i}" replaced by JS.
templ extraBodyRow(idx int, row ExtraBodyRow, readonly bool) {
	{{ idxKey := fmt.Sprintf("%d", idx) }}
	if idx == -1 {
		{{ idxKey = "{i}" }}
	}
	{{ keyName := "extra_body_k" + idxKey + "_key" }}
	{{ valName := "extra_body_k" + idxKey + "_value" }}
	<div data-xolo-row class="flex items-start gap-2">
		<div class="flex-1">
			@input.Input(input.Props{
				Name:        keyName,
				Value:       row.Key,
				Placeholder: "clé (ex : reasoning_split)",
				Disabled:    readonly,
				Attributes:  templ.Attributes{"data-xolo-name": "extra_body_k{i}_key"},
			})
		</div>
		<div class="flex-1">
			@input.Input(input.Props{
				Name:        valName,
				Value:       row.Value,
				Placeholder: "valeur (ex : true, 40, auto)",
				Disabled:    readonly,
				Attributes:  templ.Attributes{"data-xolo-name": "extra_body_k{i}_value"},
			})
		</div>
		if !readonly {
			@button.Button(button.Props{
				Type:    "button",
				Variant: button.VariantOutline,
				Size:    button.SizeIcon,
				Attributes: templ.Attributes{
					"data-xolo-row-remove": "",
					"title":                "Supprimer ce champ",
				},
			}) {
				✕
			}
		}
	</div>
}
