package component

import (
	"github.com/xolo-gateway/xolo/internal/core/model"
	common "github.com/xolo-gateway/xolo/internal/http/handler/webui/common/component"
	"github.com/xolo-gateway/xolo/internal/http/handler/webui/templui/component/form"
	"github.com/xolo-gateway/xolo/internal/http/handler/webui/templui/component/input"
)

type ProfilePageVModel struct {
	AppLayoutVModel common.AppLayoutVModel
	User            model.User
}

templ ProfilePage(vmodel ProfilePageVModel) {
	@common.AppLayout(vmodel.AppLayoutVModel, common.WithTitle("Profil")) {
		<div class="max-w-2xl">
			@common.PageHeader(common.PageHeaderProps{
				Title:    "Profil",
				Subtitle: "Votre identité vient du fournisseur d'identité de la plateforme ; elle est affichée ici en lecture seule.",
			})
			@common.Section() {
				@common.SectionHeader() {
					@common.SectionTitle() {
						Informations du compte
					}
				}
				@common.SectionBody(common.SectionBodyProps{Class: "space-y-3.5"}) {
					@form.Item() {
						@form.Label(form.LabelProps{For: "profile-name"}) {
							Nom d'affichage
						}
						@input.Input(input.Props{
							ID:       "profile-name",
							Value:    vmodel.User.DisplayName(),
							Readonly: true,
						})
					}
					@form.Item() {
						@form.Label(form.LabelProps{For: "profile-email"}) {
							Courriel
						}
						@input.Input(input.Props{
							ID:       "profile-email",
							Value:    vmodel.User.Email(),
							Readonly: true,
						})
					}
					@form.Item() {
						@form.Label(form.LabelProps{For: "profile-provider"}) {
							Fournisseur d'identité
						}
						@input.Input(input.Props{
							ID:       "profile-provider",
							Value:    vmodel.User.Provider(),
							Readonly: true,
							Class:    "font-mono",
						})
						@form.Description() {
							Ces informations sont contrôlées par votre fournisseur d'identité et ne peuvent pas être modifiées ici.
						}
					}
				}
			}
		</div>
	}
}
