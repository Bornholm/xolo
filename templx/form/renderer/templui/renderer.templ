package templui

import (
	"github.com/bornholm/go-x/templx/form"
	"github.com/xolo-gateway/xolo/internal/http/handler/webui/templui/component/checkbox"
	// The domain form package is already bound to `form`, so the templui one is
	// aliased rather than the other way around: `form.FieldContext` appears in
	// every signature of this file.
	uiform "github.com/xolo-gateway/xolo/internal/http/handler/webui/templui/component/form"
	"github.com/xolo-gateway/xolo/internal/http/handler/webui/templui/component/input"
	"github.com/xolo-gateway/xolo/internal/http/handler/webui/templui/component/selectbox"
	"github.com/xolo-gateway/xolo/internal/http/handler/webui/templui/component/textarea"
)

// Input renders a standard text input field using TemplUI components.
templ Input(fieldCtx form.FieldContext) {
	@uiform.Item() {
		if fieldCtx.Label != "" {
			@uiform.Label(uiform.LabelProps{For: fieldCtx.Name}) {
				{ fieldCtx.Label }
				if fieldCtx.Required {
					<span class="text-destructive">*</span>
				}
			}
		}
		@input.Input(input.Props{
			ID:          fieldCtx.Name,
			Name:        fieldCtx.Name,
			Type:        input.Type(fieldCtx.Type),
			Value:       getFirstValue(fieldCtx.Value),
			Placeholder: fieldCtx.Placeholder,
			Disabled:    isDisabled(fieldCtx),
			Class:       errorClass(fieldCtx),
			HasError:    fieldCtx.Error != "",
			Attributes:  extraAttrs(fieldCtx),
		})
		@fieldFeedback(fieldCtx)
	}
}

// Textarea renders a textarea field using TemplUI components.
templ Textarea(fieldCtx form.FieldContext) {
	@uiform.Item() {
		if fieldCtx.Label != "" {
			@uiform.Label(uiform.LabelProps{For: fieldCtx.Name}) {
				{ fieldCtx.Label }
				if fieldCtx.Required {
					<span class="text-destructive">*</span>
				}
			}
		}
		@textarea.Textarea(textarea.Props{
			ID:          fieldCtx.Name,
			Name:        fieldCtx.Name,
			Value:       getFirstValue(fieldCtx.Value),
			Placeholder: fieldCtx.Placeholder,
			Disabled:    isDisabled(fieldCtx),
			HasError:    fieldCtx.Error != "",
			AutoResize:  true,
			Attributes:  extraAttrs(fieldCtx),
		})
		@fieldFeedback(fieldCtx)
	}
}

// Select renders a select dropdown using TemplUI components.
templ Select(fieldCtx form.FieldContext) {
	{{ _, isMultiple := fieldCtx.Attributes["multiple"] }}
	@uiform.Item() {
		if fieldCtx.Label != "" {
			@uiform.Label(uiform.LabelProps{For: fieldCtx.Name + "-trigger"}) {
				{ fieldCtx.Label }
				if fieldCtx.Required {
					<span class="text-destructive">*</span>
				}
			}
		}
		@selectbox.SelectBox(selectbox.Props{
			Multiple: isMultiple,
		}) {
			@selectbox.Trigger(selectbox.TriggerProps{
				ID:       fieldCtx.Name + "-trigger",
				Disabled: isDisabled(fieldCtx),
				Name:     fieldCtx.Name,
				HasError: fieldCtx.Error != "",
				Multiple: isMultiple,
			}) {
				@selectbox.Value(selectbox.ValueProps{
					Placeholder: fieldCtx.Placeholder,
				}) {
					{ getSelectedLabel(fieldCtx) }
				}
			}
			@selectbox.Content(selectbox.ContentProps{}) {
				for _, opt := range form.GetSelectOptions(fieldCtx, nil) {
					@selectbox.Item(selectbox.ItemProps{
						Value:    opt.Value,
						Selected: isSelected(fieldCtx, opt.Value),
					}) {
						{ opt.Label }
					}
				}
			}
		}
		@fieldFeedback(fieldCtx)
	}
}

// Checkbox renders a checkbox field using TemplUI components.
templ Checkbox(fieldCtx form.FieldContext) {
	@uiform.Item() {
		@uiform.ItemFlex() {
			@checkbox.Checkbox(checkbox.Props{
				ID:       fieldCtx.Name,
				Name:     fieldCtx.Name,
				Value:    "true",
				Checked:  isChecked(fieldCtx),
				Disabled: isDisabled(fieldCtx),
			})
			@uiform.Label(uiform.LabelProps{For: fieldCtx.Name}) {
				{ fieldCtx.Label }
			}
		}
		@fieldFeedback(fieldCtx)
	}
}

// FileInput renders a file input field using TemplUI components.
templ FileInput(fieldCtx form.FieldContext) {
	@uiform.Item() {
		if fieldCtx.Label != "" {
			@uiform.Label(uiform.LabelProps{For: fieldCtx.Name}) {
				{ fieldCtx.Label }
				if fieldCtx.Required {
					<span class="text-destructive">*</span>
				}
			}
		}
		@input.Input(input.Props{
			ID:         fieldCtx.Name,
			Name:       fieldCtx.Name,
			Type:       input.TypeFile,
			Class:      errorClass(fieldCtx),
			HasError:   fieldCtx.Error != "",
			Attributes: extraAttrs(fieldCtx),
		})
		@fieldFeedback(fieldCtx)
	}
}

// fieldFeedback renders the description or the validation error below a field.
// The error takes precedence: a field in error shows what to fix, not what it is for.
templ fieldFeedback(fieldCtx form.FieldContext) {
	if fieldCtx.Error != "" {
		@uiform.Message(uiform.MessageProps{Variant: uiform.MessageVariantError}) {
			{ fieldCtx.Error }
		}
	} else if fieldCtx.Description != "" {
		@uiform.Description() {
			{ fieldCtx.Description }
		}
	}
}

// Hidden renders a hidden input field.
// No templui equivalent exists — a hidden input carries a value, it is not a control.
templ Hidden(fieldCtx form.FieldContext) {
	<input
		type="hidden"
		name={ fieldCtx.Name }
		value={ getFirstValue(fieldCtx.Value) }
	/>
}

// Helper functions

// getFirstValue returns the first value from the slice or empty string.
func getFirstValue(values []string) string {
	if len(values) > 0 {
		return values[0]
	}
	return ""
}

// isDisabled checks if the field should be disabled.
func isDisabled(ctx form.FieldContext) bool {
	if disabled, ok := ctx.Attributes["disabled"].(bool); ok && disabled {
		return true
	}
	return false
}

// isChecked checks if the checkbox should be checked.
func isChecked(ctx form.FieldContext) bool {
	if len(ctx.Value) > 0 && ctx.Value[0] == "true" {
		return true
	}
	return false
}

// getSelectedLabel returns the label for the selected option.
func getSelectedLabel(ctx form.FieldContext) string {
	options := form.GetSelectOptions(ctx, nil)
	currentValue := getFirstValue(ctx.Value)
	for _, opt := range options {
		if opt.Value == currentValue {
			return opt.Label
		}
	}
	return ""
}

// isSelected checks if an option is selected.
func isSelected(ctx form.FieldContext, optionValue string) bool {
	currentValue := getFirstValue(ctx.Value)
	return currentValue == optionValue
}
